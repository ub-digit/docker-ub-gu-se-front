<p><%= link_to "Back to images", '/repository/index' %></p>

<h1>Tags for <strong><%= @repo["name"] %></strong> </h1>

<ul class="list-unstyled">
    <% @repo["tags"].each do |tag|%>
    <li><%=@repo["name"]%>:<%= tag %> <a href="#" class="btn-copy-to-clipboard" title="Copy to clipboard" data-repo="<%=@repo["name"]%>:<%= tag %>">[Copy]</a></li>
    <% end -%>
</ul>